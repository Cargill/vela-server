# yaml-language-server: $schema=https://github.com/Cargill/vela-server/releases/latest/download/schema.json

version: "1"

steps:
  - name: test
    image: golang
    ruleset:
      branch: feature/*
      event: pull_request
      path: ["src/*/*.go"]
      matcher: filepath

